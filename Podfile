# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'mac_min2' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for mac_min2
  pod 'Charts'
  target 'mac_min2Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'mac_min2UITests' do
    # Pods for testing
  end

end
